---
target: ทั้งเว็บ (Web/src/app) รอบ 2
total_score: 32
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-08-08T13-10-07Z
slug: web-src-app
---
Method: dual-agent (A: a0351e3994d1bf5e7 · B: a6d9233883829bbf5)

# Design Critique r2 — เสาร์๕มหานิยม (Web/src/app) — 32/40

Heuristics: 1:3, 2:4, 3:3, 4:3, 5:3, 6:3, 7:3, 8:3, 9:3, 10:4 = 32/40 (Good).
Previous run: 26/40. Improvements credited: search/sort/pagination + aria-live counts, mobile drawer nav, LINE-first how-to-order (10 scored 4), empty-state that carries query into chat, custom icon set replacing emoji.

Design specificity: authored, not templated — devotional register (สั่งบูชา/ปิดรายการบูชาแล้ว), custom kuman/kumaree icon set, ceremony countdown, lineage-first IA. Biggest unresolved tension: legacy igetweb imagery (watermarked composites, red promo frames, mid-page Add-Friend JPEG) inside the refined shell.

Detector: 1 static finding (side-tab globals.css:99) = false positive (blockquote). Runtime: LINE green contrast 2.3:1 white-on-#06c755 confirmed (LineButton); text-overflow hits on ProductCard price span (36-57px, plausible—needs visual check) and FAQ summaries (likely measurement quirk on wrapping text); 4x image-hover-transform on articles. Zero console/network errors on all pages.

Priority issues:
- [P1] Drawer/dropdown a11y mechanics: no focus trap/restore in MobileNav dialog (SiteNav.tsx:204), role="menu" without menuitem/arrow keys on dropdowns; HeaderSearch close drops focus to body. Fix: focus close btn on open, trap Tab, restore on close; use disclosure pattern instead of menu roles.
- [P1] Floating LINE pill occludes content while scrolling on mobile (covers card prices/headings; only /how-to-order compensates). Fix: global main bottom padding at mobile + shrink pill to icon-only after scroll.
- [P2] Master avatars circle-crop legacy banner graphics into shards on /masters & home. Fix: curated portrait field + dignified fallback.
- [P2] Product-detail legacy Add-Friend JPEG banner shouts over the real CTA; replace with native token-styled QR/social block.
- [P3] Homepage double opener (carousel + hero), carousel CLICK-HERE competes with real CTA.
- [P3] Sidebar counts stale during search (พบ 24 vs ทั้งหมด 140), no active-filter chip above grid.

Personas: Jordan — no deposit/hold policy near price; legacy banner feels "unofficial" next to scam-warning FAQ. Casey — pill covers price rows; master names truncate in 2-line clamp; carousel mis-taps. Sam — focus left behind drawer; menu roles lie; good: aria-labels, aria-live, native details, focus ring.

Cognitive load failures: homepage double opener; 14-master flat sidebar list; product-detail hierarchy (banner louder than CTA).

Questions: Where is อาจารย์เล็กเสาร์ห้า (the person is the guarantee)? Is the carousel earning its place? Could sold-out pages become lineage-archive pages?
